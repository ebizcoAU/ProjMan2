# ProjMan2 Dashboard (:5110) — DEVELOPMENT template. Copy to .env.development if
# any override is ever needed — currently the dashboard needs no env vars at all;
# next.config.js's API_URL default comes from server/shared/constants.js.
API_URL=
